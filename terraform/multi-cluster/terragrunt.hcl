# Optionally, define any global inputs if needed
inputs = {
  # Global variable defaults can be specified here.
  # These can be overridden in child Terragrunt configurations.
  projectId = "connectivity-test-server"
  region    = "europe-west1"
}
